import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 15,
    gap: 50,
  },
  header: {
    alignItems: 'center'
  },
  body: {},
  footer: {
    gap: 15,
  },
});

export default styles;
