import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 15,
    marginBottom: 200,
    gap: 50,
  },
  header: {
    alignItems: 'center'
  },
  body: {},
  footer: {},
});

export default styles;
