import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent:
            'center',
        padding: 15,
    },
    text: {
        textAlign: 'center',
        fontSize: 50,
        color: '#010128'
    }
});

export default styles;